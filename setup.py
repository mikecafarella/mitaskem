from setuptools import setup, find_packages

setup(
    name="mitaskem",
    version="1.1.0",
    description="mit askem repo",
    packages=find_packages(),
    # install_requires=[],  # installed from requirements.txt
    python_requires=">=3.10"
)
